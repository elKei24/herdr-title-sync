id = "elkei24.title-sync"
name = "title-sync"
version = "1.0.0"
min_herdr_version = "0.8.0"
platforms = ["macos", "linux"]
description = "Mirror each agent's terminal title onto its herdr tab label."

[[startup]]
command = ["python3", "herdr/daemon.py"]
